Replicas: 1
ExpectedClusterSize: 1
RevisionHistoryLimit: 3

Image:
  repository: localhost
  name: host-app
  tag: v1
  pullPolicy: Never

name: host-app
namespace: default

service:
  type: ClusterIP
  port: 3001
